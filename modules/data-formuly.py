from bottle import response, request
import datetime
import binascii

encuestas[]
def get_encuesta_usuario(username,genero,fecha,correo,id):
    encuestas = {
    "username" : username,
    "genero" : genero,
    "fecha"fecha : ,
    "correo" : correoel,
    "ID" : id,
    }
encuestas.append(encuestas)
return json.dumps(encuestas)
